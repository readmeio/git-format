export const MyComponent = ({ title = "Single-File Component", ...props }) => (
  <article className="MyComponent">
    <h1>{title}</h1>
    <section>
      {props.children}
    </section>
  </article>
);

export default MyComponent;
